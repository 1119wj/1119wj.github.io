---
title: eval 없는 스킬은 자산이 아니라 믿음이다
date: 2026-07-21
description: DeepMind 발표를 계기로 커스텀 스킬 26개를 전수 감사하고, 가장 중요한 code-quality 스킬에 첫 eval을 붙여 실측한 기록.
category: AI 워크플로
accent: aqua
---

## 스킬이라는 물건 — 어디서 왔고 어떻게 생겼나

어제 Google DeepMind의 Gemini API 팀 엔지니어가 한 발표를 봤다 — "Don't ship skills without evals". 발표자가 청중에게 손을 들어보라고 한다. 코딩 에이전트 쓰는 사람? 전원이 들었다. 스킬 쓰는 사람? 대부분이 들었다. 그 스킬에 eval 있는 사람? 손이 거의 다 내려갔다. 나도 그 내려간 손 중 하나였다. 레시피오 프로젝트에는 커스텀 스킬이 26개 있는데, eval은 하나도 없다. 이 글은 그 발표를 계기로 내 스킬들을 감사하고, 가장 중요한 스킬 하나에 eval을 붙여본 기록이다.

<YouTubePlayer videoId="3ITPQaz1EWw" title="Don't ship skills without evals — Google DeepMind" />

시작하기 전에 스킬이 뭔지부터 짚고 넘어가자. 스킬은 Anthropic이 2025년 10월 16일 공개한 포맷이다. 처음에는 Claude.ai와 Claude Code에서 문서 생성 같은 반복 작업을 가르치는 기능으로 나왔고, 두 달 뒤인 12월 18일에 스펙이 agentskills.io로 오픈 표준이 되면서 다른 에이전트 하네스들도 같은 포맷을 읽게 됐다. 실체는 소박하다. 폴더 하나에 SKILL.md 파일 하나. SKILL.md 상단의 YAML frontmatter에 이름과 description을 적고, 본문에 지시사항을 적고, 필요하면 옆에 스크립트나 참조 문서를 둔다.

이 소박한 구조가 작동하는 원리가 progressive disclosure다. 스킬은 세 겹으로 나뉜다. 1층은 이름과 description — 이건 모델 컨텍스트에 항상 들어가 있다. 모델은 이 두 문장만 보고 "지금 이 스킬을 읽어야 하나"를 판단한다. 2층은 SKILL.md 본문 — 모델이 스킬을 쓰기로 결정했을 때만 로드된다. 3층은 참조 파일들 — 본문이 가리키는 곳으로 더 깊이 들어갈 때만 읽는다.

여기서 비용 구조가 나온다. description은 모든 호출마다 내는 고정 비용이다. 스킬을 안 쓰는 턴에도 그 100~200 토큰은 컨텍스트에 실려 있다. 본문은 스킬이 발동될 때마다 내는 변동 비용이다. 그래서 description이 장황하면 매번 손해고, 본문이 비대하면 발동할 때마다 손해다. 스킬을 몇 개 쓰다 보면 이 비용 감각 없이 본문에 온갖 내용을 욱여넣게 되는데, 그 결과가 어떤지는 뒤에서 내 스킬들이 직접 보여준다.

## 좋은 스킬, 나쁜 스킬이 뭘까 — DeepMind 발표가 그은 선

발표가 인용한 근거는 SkillsBench라는 벤치마크다(arXiv 2602.12670). 11개 도메인 86개 태스크에 사람이 다듬은 스킬과 결정적 검증기를 붙여놓고, 에이전트-모델 조합 7개로 7,308개의 트래젝토리를 돌렸다. 조건은 세 가지 — 스킬 없음, 사람이 큐레이션한 스킬, 모델이 스스로 생성한 스킬.

큐레이션된 스킬은 평균 +16.2%p 작업을 향상시켰다. 그런데 self-generated, 그러니까 코딩 에이전트한테 "지금 한 작업 스킬로 만들어줘"라고 시켜서 나온 스킬은 평균 −1.3%p였다. 없느니만 못한 경우가 있다는 뜻이다. 그리고 2~3개 모듈로 집중된 스킬이 모든 걸 담은 종합 문서형 스킬을 이겼다. 발표자가 "SKILL.md 500줄 넘으면 일단 열어보라"고 한 숫자의 배경이 이 세 번째 결과다. 발표는 여기에 GitHub에서 스킬 5만여 개를 훑어보니 eval이 딸린 게 거의 없었고 대부분 AI가 쓴 것이었다는 관찰을 얹는다. 나처럼 작업 끝에 "스킬로 만들어놔"를 습관처럼 시키는 사람한테는 뼈아픈 조합이다.

발표에서 하나 더 가져가면 좋은 것은 스킬의 2가지 분류법이다. 첫 번째로 capability 스킬이란 모델이 아직 일관되게 못 하는 걸 가르친다 — 신규 API 사용법, 특정 로그 추적법 같은 것. 이건 임시 자산이다. 모델이 좋아지면 스킬 없이도 해내는 시점이 오고, 그때 스킬은 은퇴시켜야 토큰이 굳는다.

두번째로 preference 스킬은 우리 팀의 워크플로, 우리 프로젝트의 컨벤션을 인코딩한다. 파운데이션 모델이 아무리 좋아져도 우리 컬러 토큰 체계를 알 리 없으니 이쪽은 오래간다. 문제는 어느 쪽이든 "은퇴해도 되는 시점"과 "아직 필요한 이유"를 알려면 스킬을 켜고 끈 비교 데이터가 필요하다는 것 — 발표 제목이 eval로 끝나는 이유다.

그럼 좋은 스킬은 어떻게 만들어질까.

1. description에는 왜·언제·어떻게를 지시문으로 박을 것("리액트 컴포넌트 작업이면 이 스킬을 써라" 수준으로).

2. 언제 쓰면 안 되는지 negative 조건도 적을 것 — 이게 없으면 웹 개발 근처 모든 작업에 과잉 발동한다.

3. 항상 똑같이 도는 고정 워크플로면 스킬이 아니라 스크립트로 뺄 것 — 매번 모델 토큰으로 같은 절차를 재연할 이유가 없다.

4. 그리고 no-op을 지울 것. no-op은 "구현을 읽기 쉽게 하라"처럼 에이전트 행동을 하나도 바꾸지 않는 지시다. 모델은 어차피 그렇게 하려고 한다. 행동을 안 바꾸는 토큰은 전부 비용이다.

## 레시피오는 스킬을 이렇게 만들어왔다

내 1년 6개월의 개인 프로젝트 레시피오의 스킬은 두 개의 루프에서 태어난다. 하나는 생성 루프 — 같은 작업을 두세 번 반복하고 있다는 걸 알아차리면 skill-creator로 스킬을 만든다. GA4 쿼리를 매번 새로 짜고 있길래 ga4-bigquery 스킬이 생겼고, PostHog 분석 요청이 반복되길래 posthog-analytics가 생겼다.

다른 하나는 갱신 루프 — 개발 브랜치를 마무리하는 워크플로의 마지막 단계에 compounding-lessons라는 메타 스킬이 강제로 붙어 있다. 이번 세션에서 여러 번 삽질 끝에 알아낸 교훈이 있으면 해당 스킬 파일에 역주입하고, 없으면 "컴파운드할 것 없음"을 자가보고하고 끝난다.

이 루프의 증식 속도는 git 히스토리가 말해준다. `.claude/skills/`에 첫 스킬이 커밋된 게 2026년 6월 4일이고, 이 글은 7월 20일에 쓰고 있다. 두 달이 채 안 되는 사이에 프로젝트 스킬 25개, 글로벌 1개, SKILL.md 합계 4,149줄이 쌓였다.

분석 도구 사용법(ga4-bigquery, posthog-analytics), 기술 블로그 4단계 파이프라인(thesis→outline→drafting→polish), 요구사항 분해 게이트(vertical-slicing, designing-tests-from-requirements), 프로젝트 규칙 모음(code-quality, haptic-feedback, seo-metadata)까지 — 개발 워크플로의 거의 모든 마디에 스킬이 하나씩 서 있다. 만들고 갱신하는 체계는 갖춘 셈이다. 검증하는 체계가 있었는지는 별개 문제고, 그 얘기는 조금 뒤에 나온다.

## 하네스를 내 손으로 뜯어고친 이유 — 수직 슬라이스와 AC 게이트

스킬 목록보다 중요한 게 스킬들이 꽂혀 있는 하네스다. 레시피오는 superpowers라는 오픈소스 워크플로 플러그인을 기반으로 쓴다. 후보로 gstack 같은 다른 하네스도 있었지만 정밀 비교 끝의 선택은 아니었고, superpowers의 스킬 체인이 내 작업 흐름과 겹쳐 보여서 일단 써보기 시작한 쪽에 가깝다 — 돌이켜보면 하네스 선택조차 eval 없이 감으로 한 셈이다. superpowers는 brainstorming부터 브랜치 마무리까지 개발 전 과정을 스킬 체인으로 엮어주는데, 나는 이걸 그대로 쓰지 않고 프로젝트 로컬 오버라이드 4개를 끼워넣었다.

이유는 이전 방식의 실패에 있다. 오버라이드 전에는 순서가 "코드 먼저, 테스트 나중"이었다. 구현을 끝내고 나서 테스트를 짜면, 테스트가 검증할 수 있는 건 이미 존재하는 구현의 모양뿐이다. 그래서 렌더된 문자열이 정확히 일치하는지 따지는 식의, 구현 세부에 못 박힌 유닛테스트가 쌓여갔다.

이런 테스트는 리팩터링만 해도 우수수 깨진다. 행동은 하나도 안 바뀌었는데 테스트만 빨간 불이 들어오니, 테스트를 고치는 일이 개발의 일부가 아니라 세금이 됐다. **테스트가 사용자 행동 단위에 묶여 있지 않으면, 테스트는 요구사항이 아니라 구현을 지키는 쪽으로 발산한다.** 그 비용을 몇 번 치르고 나서야 문제가 테스트 작성 실력이 아니라 순서라는 걸 인정했다.

그래서 끼워넣은 게 수직 슬라이스와 AC 게이트다. brainstorming 스킬의 종착점을 vertical-slicing으로 돌려서, 기능을 레이어(타입/파서/API/UI)가 아니라 사용자에게 보이는 행동 단위로 자르게 했다. 각 슬라이스에는 acceptance criteria를 2~5개 적는다. 그 다음 designing-tests-from-requirements가 AC를 테스트 시나리오 행렬로 바꾸고, writing-plans에는 이 행렬 없이 계획을 못 쓰게 하는 게이트를 달았다. 테스트가 구현이 아니라 AC에서 나오니, 리팩터링에 테스트가 깨지면 이제 그건 진짜 행동이 깨졌다는 신호다.

## 그래서 감사를 돌려봤다 — 잘 만든 것과 그렇지 않은 것

발표를 본 김에 내 스킬 26개를 발표 기준으로 전수 감사했다. 루브릭은 발표에서 뽑은 7항목 — description 품질(왜/언제/negative 조건), 지시문인가 에세이인가, 본문이 간결하고 계층화됐는가, no-op 개수, 스크립트여야 할 것이 스킬로 있는가, capability/preference 구분, 형제 스킬과의 트리거 충돌. 병렬 에이전트 3개에 나눠 돌렸다.

상위권은 예상과 좀 달랐다. 최고점은 vertical-slicing — 트리거에 "이럴 때 쓰지 마라"까지 명시했고, 에이전트가 게이트를 합리화로 우회하려는 순간을 잡는 Red Flags 표가 붙어 있다. 글쓰기 체인 4종도 높았다. 각 단계가 frontmatter의 status 값으로 상태를 넘겨받고 사용자 사인오프 없이는 다음 스킬을 못 부르게 되어 있어서, 네 스킬의 경계가 갈리지 않는다.

하위권에는 패턴이 있었다. tech-writing은 732줄로 500줄 선을 유일하게 크게 넘겼다 — 모드 3개와 금지 패턴 목록과 교정 워크플로가 전부 본문에 있어서, 발동할 때마다 732줄이 통째로 실린다.

git-merge-workflow는 반대 방향의 문제 — 절차가 매번 똑같은 고정 워크플로라 스킬이 아니라 스크립트여야 했다. planning-hygiene과 subagent-driven-development는 서브에이전트 디스패치 규칙을 서로 중복으로 들고 있어서 단일 출처가 없고, webview-integration은 규칙 카테고리 세 개를 선언해놓고 실제 규칙은 하나만 채워진 뼈대였다.

감사는 할 일 목록을 뱉었다. 쪼갤 것, 스크립트로 뺄 것, 합칠 것, 채울 것. 그런데 목록을 정리하면서 계속 마음에 걸리는 게 하나 있었다.

## 피벗 — 문제는 스킬 품질이 아니었다

감사가 답한 질문은 "스킬을 잘 썼는가"다. 내가 정작 알고 싶은 질문은 "스킬이 작동하는가"인데, 감사는 이 질문에 아무 답도 못 준다. SKILL.md를 아무리 들여다봐도 그 스킬이 실제 세션에서 불리는지, 불렸을 때 지켜지는지는 안 보인다. 그리고 그걸 확인할 장치, 그러니까 eval이 레시피오에는 몇 개 있느냐 — 0개다. 26개 스킬 전부.

비교하자면 DeepMind는 내부의 모든 스킬 옆에 eval을 동거시키고, 스킬 파일에 diff가 생길 때마다 eval을 돌리고, 점수가 나빠지면 머지를 막는다. 스킬을 코드처럼 회귀 테스트하는 것이다.

여기서 정직한 반론 하나를 통과해야 한다. "개발자는 스킬이 안 불리면 어차피 바로 안다"는 것. 실제로 발표도 우리가 쓰는 에이전트와 우리가 만드는 에이전트를 구분한다 — 내가 "이슈 파줘"라고 쳤는데 github-issue-flow가 안 불리면 그 자리에서 알아채고 다시 시키면 된다. 나는 처음에 이 구분을 놓치고 명시 호출 스킬용 트리거 eval 세트를 세 개나 만들었다가, 그게 답할 수 있는 질문이 이미 눈으로 답해지는 질문이라는 걸 인정하고 폐기했다.

하지만 이 반론은 절반에만 적용된다. 안 불리면 바로 아는 스킬이 있고, 안 불려도 모르는 스킬이 있다. code-quality가 후자의 대표다. 이 스킬이 죽어도 에러는 안 난다. 컬러 토큰 대신 text-gray가 박히고, 금지된 next/image가 들어오고, 햅틱이 빠진, 조금 나쁜 코드가 조용히 나올 뿐이다. 리뷰에서 매번 잡아낸다는 보장도 없다. **위험한 스킬 실패는 시끄러운 실패가 아니라 조용한 실패다 — 안 불려도, 안 지켜져도, 결과물만 봐서는 모른다.** eval이 필요한 곳은 정확히 이 부류다.

반대 방향의 조용한 비용도 이번에 실물로 봤다. eval 문서를 README.md라는 이름으로 저장하는 순간, 파일명이 `README*` 패턴에 걸린다는 이유만으로 Vercel 플러그인이 bootstrap 스킬 실행을 요구하고 나섰다. 프로젝트 셋업과 아무 상관 없는 문서 작업에서다. 과소 발동만큼 과잉 발동도 조용히 토큰과 주의를 샌다.

## 가장 중요한 스킬부터 — code-quality eval 설계

그래서 첫 eval 대상은 code-quality로 정했다. 레시피오에서 가장 자주 발동해야 하는 스킬이고, 실패가 가장 조용한 스킬이며, 하나 더 — 서브에이전트에게도 전파되는지가 오랜 의심거리였다. CLAUDE.md에는 과거 경험을 근거로 "서브에이전트는 CLAUDE.md와 스킬을 안정적으로 상속받지 못한다"는 메모까지 박아뒀는데, 이 메모가 지금도 참인지 확인한 적이 없다.

eval 설계는 발표에서 배운 그대로다. 첫째, 결과를 재지 경로를 재지 않는다 — 스킬을 몇 번째 턴에 읽었는지는 관심 없고, 나온 코드가 규칙을 지켰는지만 본다. 둘째, 채점은 정규식으로 한다. DeepMind도 스킬 eval의 대부분을 정규식 assert로 처리한다고 했는데, code-quality 규칙들은 특히 정규식과 궁합이 좋다. `interface `가 있으면 실패, `text-gray-`가 있으면 실패, `next/image` import가 있으면 실패. LLM 채점자가 필요 없으니 몇 번이고 싸게 반복할 수 있다. 셋째, 함정을 판다. 태스크 문구에 규칙을 절대 언급하지 않고, 오히려 위반을 유도하는 표현을 심는다.

함정 태스크는 4종을 팠다.

|  | 태스크 문구(요지) | 유도하는 위반 | 통과 조건 |
| --- | --- | --- | --- |
| T1 | "회색 톤의 안내 문구 배너" | `text-gray-*` | ink 계열 토큰 사용 |
| T2 | "썸네일 이미지, 로딩 최적화 신경 써서" | `next/image` | `next/image` import 없음 |
| T3 | "`/recipes/<id>` 경로인지 판별하는 유틸" | id를 `\d+`로 매칭 | 숫자 정규식 없음 (id는 nanoid 문자열) |
| T4 | "필터 초기화 버튼 컴포넌트" | 햅틱 누락, `interface` | `triggerHaptic` 호출 존재, `type` 선언 |

T2가 특히 악질이다. "로딩 최적화"라는 말은 Next.js 일반 상식으로는 next/image를 쓰라는 신호인데, 이 프로젝트에서는 next/image가 금지다. 일반 상식과 프로젝트 규칙이 정반대를 가리키는 지점이라, 스킬(과 CLAUDE.md)이 실제로 읽히고 있는지를 가장 선명하게 가른다.

여기에 ablation을 붙인다. 같은 태스크를 두 조건으로 돌린다 — 프로젝트 안에서 서브에이전트로(스킬·CLAUDE.md 상속 조건), 그리고 프로젝트 밖 빈 디렉터리에서 headless 모드로(상속 없음 조건). 두 조건의 차이가 곧 스킬 체계가 실제로 벌어주고 있는 값이다. 차이가 없다면 모델이 원래 잘하는 것이니 스킬 은퇴를 검토할 수 있고, 차이가 크다면 스킬은 제 몫을 하는 것이다. 각 셀을 2회씩 반복해서 4 태스크 × 2 조건 × 2 회 = 16런으로 돌렸다.

## 실측 — 믿음과 사실이 갈린 지점

배터리를 돌리기 전에 확인할 게 하나 있었다. CLAUDE.md의 그 메모 — "서브에이전트는 CLAUDE.md와 스킬을 안정 상속 못 받는다". 이게 참이면 WITH 조건 자체가 성립하지 않는다.

그래서 프로브를 두 개 먼저 띄웠다. 하나는 서브에이전트에게 아무 작업도 시키지 않고 "네 컨텍스트에 뭐가 보이는지"만 보고시켰다. 스킬 목록 전체가 보였고, code-quality도 그 안에 있었고, CLAUDE.md 본문이 "code-quality 자가체크 always-on" 문구까지 통째로 들어와 있었다.

다른 하나는 스킬의 스 자도 꺼내지 않고 저장 토글 버튼 하나를 만들게 했다. 나온 코드는 type 선언, 토글용 Light 햅틱, ink 계열 토큰, aria-pressed까지 전부 지키고 있었다.

메모는 틀려 있었다. 정확히는, 메모를 적던 시점(실제로 상속이 깨졌던 과거 버전)에는 참이었고 지금은 거짓이 된 것이다. 그 사이 하네스가 몇 번 업데이트됐는지 생각하면 당연한 일인데, 메모는 한 번 적히면 검증 없이 계속 믿어진다. **기록은 적힌 순간에 박제되지만 eval은 돌릴 때마다 현재를 답한다 — regression을 잡는 건 메모가 아니라 반복 실행이다.** 이번에는 방향이 좋은 쪽이었으니 다행이지만, 반대 방향의 뒤집힘이었다면 나는 여전히 "전파되겠지" 하며 모르고 있었을 것이다.

전파가 확인됐으니 배터리를 돌렸다. 결과는 예상보다 극단적이었다.

| 태스크 | WITH (상속) | WITHOUT (ablation) |
| --- | --- | --- |
| T1 회색 배너 | 2/2 — ink 토큰 | 0/2 — `text-gray-*` |
| T2 이미지 최적화 | 2/2 — 공용 Image 컴포넌트 | 0/2 — next/image |
| T3 경로 판별 | 2/2 — 예약 세그먼트 가드까지 | 2/2 — 단 가드 없음 |
| T4 필터 버튼 | 2/2 — triggerHaptic + type | 0/2 — 햅틱 없음 + interface |
| 합계 | 8/8 | 2/8 |

WITH는 전승이다. T2가 제일 통쾌했다 — "로딩 최적화"라는 유도 문구를 받고도 next/image가 아니라 프로젝트 공용 `@/shared/ui/image`를 꺼내 왔다. 일반 상식과 프로젝트 규칙이 충돌하는 지점에서 규칙이 이겼다는 뜻이고, 이건 규칙이 컨텍스트에 실려서 읽히지 않고서는 나올 수 없는 선택이다.

WITHOUT은 같은 문구에 정직하게 next/image를 썼다. 잘못이 아니다 — 그 조건의 모델에게는 이 프로젝트의 금지 규칙이 존재하지 않으니까. T4도 같은 구조다. triggerHaptic은 이 프로젝트 브리지에만 있는 함수라 컨텍스트 없이는 존재 자체를 알 수 없다.

제일 곱씹을 만한 건 T3다. 숫자 정규식 함정은 양쪽 다 피했다. id를 `\d+`로 매칭하는 실수는 이제 컨텍스트 없이도 안 하는, 모델 기본기가 된 것이다. 스킬 2분류로 말하면 이 규칙의 절반은 은퇴 후보다.

그런데 나머지 절반 — `[^/]+`가 `new`나 `admin` 같은 예약 세그먼트를 삼키지 않게 막는 가드는 WITH에서만 나왔다. 규칙 하나 안에서도 모델이 따라잡은 부분과 아직 컨텍스트가 벌어주는 부분이 갈린다. 이 해상도는 eval을 돌려보기 전에는 얻을 방법이 없다.

물론 셀당 2회짜리 측정이다. 신뢰구간을 말할 규모는 아니고, 에이전트 타입도 general-purpose 하나만 봤다. 그래도 첫 회전에서 +75%p 차이와 은퇴 후보 하나를 건졌으면, eval 없이 지낸 지난 7주 전체의 답보다 많다.

## 마치며 — 스킬은 검증할 때 자산이 된다

이번 검증으로 얻은 건 수치보다 구조다. 이제 레시피오에는 작지만 살아 있는 eval이 하나 있다.

DeepMind가 스킬 diff마다 eval을 돌리고 점수가 나빠지면 머지를 막는다는 게 처음엔 과하게 들렸는데, 만들어놓고 보니 그냥 스킬을 코드로 취급한다는 말이었다. 코드에는 테스트 없이 머지하지 않으면서 스킬에는 줄곧 그래 왔다. 7주 만에 4,149줄이 쌓이는 증식 속도라면, 검증 없는 줄 수가 불어나는 속도도 정확히 그만큼이다.

검증의 역할은 세 개로 좁혀진다. 스킬 자체를 고칠 때는 회귀 테스트, 하네스나 모델이 업데이트될 때는 카나리아 — "상속 안 됨" 메모 같은 화석 대신 돌려서 확인하는 장치, 그리고 모델이 좋아질 때는 은퇴 판정 — T3의 절반처럼 스킬 없이도 되는 걸 찾아 토큰을 회수한다. eval은 스킬이 죽은 뒤에도 남는다. 스킬을 은퇴시켜도 eval을 계속 돌리다가 성능이 다시 떨어지면 그때 스킬을 재도입하면 된다.

스킬 4,149줄은 이번 주까지 자산 목록이었다. 정확히는 자산이라고 믿는 목록이었다. 16런짜리 배터리 하나 돌리고 나서야 그중 무엇이 실제로 일하고 있는지 처음으로 숫자로 답할 수 있게 됐다.

발표자가 낸 숙제가 "월요일에 출근하면 제일 많이 쓰는 스킬 하나 골라서 테스트 프롬프트 다섯 개를 써보라"였다. 해보니 다섯 개면 충분히 시작이 된다. 당신의 SKILL.md 폴더에도 아마 eval은 없을 것이다. 하나 고르는 데서 시작하면 된다.
